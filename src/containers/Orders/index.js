import React from 'react';

const Orders = () => {
  const style = {
    width: '50%',
    margin: '0 auto',
    padding: ' 100px 20px',
    textAlign: 'center',
    fontSize: '30px'
  };

  return <p style={style}>Orders Page - this page is under construction.</p>;
};

export default Orders;
